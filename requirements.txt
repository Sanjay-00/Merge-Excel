streamlit==1.38.0
# streamlit 1.38.0 requires pillow<11,>=7.1.0; pinned to the newest release
# in that range so the resolver can't fall back to an old Pillow with no
# prebuilt wheel for the deploy host's Python, which forces a from-source
# build that fails without a C toolchain.
pillow==10.4.0
pandas>=2.2.0
openpyxl>=3.1.2
xlsxwriter>=3.2.0
xlrd>=2.0.1
pyxlsb>=1.0.10
pdfplumber>=0.11.0
img2table>=1.4.0
# OCR for scanned PDFs (PDF to Excel tool); optional but strongly recommended
rapidocr>=3.0.0
onnxruntime>=1.17.0
